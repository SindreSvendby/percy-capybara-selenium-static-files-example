gem install bundler
bundle
echo "Download geckoboard driver if not present in your PATH"
echo "If now present this gist will fail with the following error message 'Selenium::WebDriver::Error::WebDriverError:'"
echo "Gecodriver can be downloaded from https://github.com/mozilla/geckodriver/releases. remeber to place it in your PATH afterwords."
echo "To check if in path run 'which geckodriver' or just simply 'geckodriver' and see if it's starts."
